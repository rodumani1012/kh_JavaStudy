
DROP SEQUENCE MYSEQ;
DROP TABLE MYBOARD;

CREATE SEQUENCE MYSEQ;

--번호, 이름, 제목, 내용, 날짜
CREATE TABLE MYBOARD(
	MYNO NUMBER PRIMARY KEY,
	MYNAME VARCHAR2(500) NOT NULL,
	MYTITLE VARCHAR2(2000) NOT NULL,
	MYCONTENT VARCHAR2(4000) NOT NULL,
	MYDATE DATE NOT NULL
);

INSERT INTO MYBOARD(MYNO, MYNAME, MYTITLE, MYCONTENT, MYDATE)
VALUES(MYSEQ.NEXTVAL,'관리자','TEST 제목','TEST 글 1234',SYSDATE);

SELECT * FROM MYBOARD;