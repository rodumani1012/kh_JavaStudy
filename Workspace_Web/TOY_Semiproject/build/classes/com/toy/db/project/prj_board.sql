DROP TABLE
	PRJ_BOARD;

DROP SEQUENCE BOARDSEQ;

CREATE SEQUENCE BOARDSEQ;

CREATE TABLE
	PRJ_BOARD(BOARD_NUM NUMBER PRIMARY KEY,
	PRJ_NUM NUMBER REFERENCES PRJ_INFO(PRJ_NUM) ON DELETE CASCADE,
	BOARD_TITLE VARCHAR2(100) NOT NULL,
	BOARD_PLACE NUMBER NOT NULL);

SELECT
	*
FROM
	PRJ_BOARD;

SELECT MAX(BOARD_PLACE) FROM PRJ_BOARD WHERE PRJ_NUM = 42;

SELECT * FROM PRJ_BOARD;

SELECT
	*
FROM
	PRJ_BOARD
WHERE
	PRJ_NUM = 7;
ORDER BY BOARD_PLACE;

DELETE
FROM
	PRJ_BOARD
WHERE
	BOARD_NUM NOT IN(66,
	67,
	68);
